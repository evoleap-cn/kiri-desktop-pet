# 开发日志 2026-04-11

## 项目：kiri-desktop-pet 二次开发

### 分支
`dev/feature`（从 `main` 切出）

---

### 变更记录

#### 1. 新建开发分支
- 从 `main` 切出 `dev/feature` 分支，用于二次开发

#### 2. 删除动画与逃跑能力，替换宠物外观
- 删去所有动画状态机（behavior engine、state machine、sleep/wake 序列等）
- 删去鼠标靠近后宠物自动逃跑的逻辑
- 宠物外观替换为 EvoLeap 品牌图标（`资源 1.svg`：白色圆角方块 + 蓝色 E 字母）
- 窗口尺寸改为 80×80

#### 3. 透明区域点击穿透
- 使用 Canvas 对 SVG 做逐像素 alpha 检测
- 不透明像素（图标本体）可交互/拖拽
- 透明像素点击穿透到底层窗口

#### 4. 文件改动
| 文件 | 变更 |
|------|------|
| `src/main.js` | 重写，精简至 ~130 行，去除行为引擎/HTTP服务/Linux监控 |
| `src/preload.js` | 重写，仅暴露拖拽相关 IPC |
| `src/index.html` | 重写，去除旧 SVG object 标签和帽子层 |
| `src/renderer.js` | 重写，内联 SVG + Canvas hit-test + 拖拽逻辑 |
| `src/renderer-rich.js` | 不再使用（保留未删） |
| `assets/svg/evoleap-logo.svg` | 新增，复制自 `D:\Steve\Download\SVG\资源 1.svg` |

#### 5. 确认运行正常
- `npm start` 目视验证：图标显示正确，透明区域穿透正常

#### 6. 左键菜单（Popup）
- 点击宠物（非拖动）弹出功能菜单，5秒无操作自动关闭
- 菜单项：Evo快捷框、Evo读屏、Evo网盘、录音纪要、截屏提问
- 底部搜索栏："向Evo提问"
- 使用独立 BrowserWindow 实现，透明、无边框、始终置顶
- 定位逻辑：优先左上方（popup.bottom=pet.top, popup.right=pet.right），备选右下方

#### 7. 右键菜单（Context Menu）
- 使用 Electron 原生 `Menu.popup()` 实现
- 菜单项：Reset Position（重置位置到右下角）、Quit（退出）

#### 8. 窗口样式优化
- 两个窗口均设置 `type: "toolbar"` + `skipTaskbar: true`，隐藏任务栏图标
- padding 设为 16px

#### 9. 修复点击穿透 Bug
- `destroyPopup()` 统一负责恢复鼠标状态：`setIgnoreMouseEvents(false)` + 发送 `popup-closed` IPC
- 渲染进程监听 `popup-closed` 重置 `lastIgnore` 缓存
- 所有关闭路径（blur/toggle/菜单选项）都走 `destroyPopup()`

#### 10. 配置 /devlog 技能
- 创建 `.claude/skills/devlog.md` 技能定义
- 自动记录开发进度到 `devlog/Steven/YYYY-MM-DD.md`
